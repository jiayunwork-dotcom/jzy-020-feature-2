package com.flamelab.inverse;

/**
 * One branch of the flame-temperature curve T(phi) relative to its peak:
 * LEAN is the phi-ascending side below the peak equivalence ratio, RICH the
 * phi-descending side above it.
 */
public enum Side {
    LEAN,
    RICH
}
